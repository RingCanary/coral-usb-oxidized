# Word-Patch v2 Pi5 Validation (Holdout 8976/2352) — 2026-03-01

## Scope
Validate Rust-generated v2 field-specs/patchspecs on the true holdout family:
- Family: `EO=8976`, `PC=2352`
- Endpoints: `896` + `2688`
- Holdout target: `1792`
- Device: Pi5 + Coral USB (reboot-first per case)

## Inputs
Generated by Rust tool (`src/bin/word_field_spec_v2.rs`):
- `traces/analysis/specv2-holdout-family8976-20260301T2030Z/pc_v2.fieldspec.json`
- `traces/analysis/specv2-holdout-family8976-20260301T2030Z/eo_v2.fieldspec.json`
- `traces/analysis/specv2-holdout-family8976-20260301T2030Z/pc_v2.rust.patchspec` (`51` bytes)
- `traces/analysis/specv2-holdout-family8976-20260301T2030Z/eo_v2.rust.patchspec` (`10` bytes)
- `traces/analysis/specv2-holdout-family8976-20260301T2030Z/both_v2.rust.patchspec` (`61` bytes)

Offline mismatch improvements (vs holdout target bytes):
- PC: `114 -> 51`
- EO: `156 -> 10`

## Reboot-First Matrix #1
Run dir:
- `traces/analysis/specv2-reboot-matrix-20260301T150957Z/`

Results:
- `baseline.log`: **PASS**
  - output hash: `0x67709fedfd103a2d`
- `pc_v2.log` (`51` PC bytes): **FAIL**
  - reaches run phase, then `Event read failed ... Timeout`
- `eo_v2.log` (`10` EO bytes): **FAIL**
  - completion event observed, then `UsbError(Timeout)` on output
- `both_v2.log` (`61` bytes): **FAIL**
  - same as PC-side failure (`Event read failed ... Timeout`)

Interpretation:
- PC failure mode moved from prior class-2 admission wall to run/event timeout.
- EO retains event-then-output-timeout behavior.

## Reboot-First Matrix #2 (Coarse Splits)
Run dir:
- `traces/analysis/specv2-subset-reboot-matrix-20260301T151352Z/`

Patch splits:
- PC: `first26` and `last25`
- EO: `first5` and `last5`

Results:
- baseline: **PASS** (`0x67709fedfd103a2d`)
- `pc_first26`: **FAIL** (`Event read failed ... Timeout`)
- `pc_last25`: **FAIL** (`Event read failed ... Timeout`)
- `eo_first5`: **FAIL** (event then output timeout)
- `eo_last5`: **FAIL** (event then output timeout)

Interpretation:
- PC toxicity is distributed (both halves can fail independently).
- EO toxicity is also distributed across both halves.

## Reboot-First Matrix #3 (EO Single-Byte Ablation)
Run dir:
- `traces/analysis/specv2-eo-single-reboot-matrix-20260301T151811Z/`

Summary:
- baseline: output hash `0x67709fedfd103a2d`
- Single-byte EO patches causing **event+timeout**:
  - offset `746`
  - offset `975`
  - offset `1103`
  - offset `1231`
- Single-byte EO patches that **complete output**:
  - `615` -> hash `0x08c23dd45f0394f4`
  - `642` -> hash `0xd1acb1e10033b9a5`
  - `839` -> hash `0x355723cc18b3264c`
  - `866` -> hash `0xbab026191ca608bd`
  - `6723` -> hash `0x67709fedfd103a2d` (baseline hash)
  - `6726` -> hash `0x67709fedfd103a2d` (baseline hash)

Interpretation:
- EO timeout behavior is driven by a compact toxic subset (`746, 975, 1103, 1231`).
- Other EO bytes perturb output semantics but are transport-safe.

## Reboot-First Matrix #4 (EO Toxic vs Non-Toxic Set)
Run dir:
- `traces/analysis/specv2-eo-toxic-nontoxic-matrix-20260301T152600Z/`

Patchsets:
- `eo_v2_toxic4.rust.patchspec` = `{746,975,1103,1231}`
- `eo_v2_nontoxic6.rust.patchspec` = `{615,642,839,866,6723,6726}`

Results:
- baseline: **PASS** (`0x67709fedfd103a2d`)
- toxic4: **FAIL** (event then output timeout)
- nontoxic6: **PASS** output transfer
  - output hash `0xf928e7e4eb9c4eff`

Interpretation:
- The EO timeout defect is now isolated to a 4-byte toxic set.
- Remaining EO bytes are semantically active but not output-transfer-fatal.

## Current Frontier
1. **PC plane** (`51` bytes): still run/event-timeout-fatal; toxicity appears distributed.
2. **EO plane** (`10` bytes): transfer-fatal core isolated to `{746,975,1103,1231}`.

## Immediate Next RE Step
- Run PC delta-debugging (binary split + residue/bit-range grouping) to isolate minimal toxic PC subset(s) analogous to EO toxic4.
- Then recompose candidate patchsets:
  - keep only transfer-safe EO bytes first,
  - add minimal safe PC subset incrementally,
  - require both admission/event/output stability and baseline-equivalent output hash where possible.
